# Pin installs to the public registry. A user-level .npmrc pointing at a
# private mirror would otherwise leak that mirror's URLs into bun.lock,
# producing a lockfile nobody outside that network can install from.
registry=https://registry.npmjs.org/
